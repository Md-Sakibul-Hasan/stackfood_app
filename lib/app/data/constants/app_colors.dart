import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF5FA287);

  // Background Colors
  static const Color background = Color(0xFFF8F9FA);
  static const Color cardBackground = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF2B3D4F);
  static const Color textSecondary = Color(0xFF8B9196);
  static const Color textLight = Color(0xFFB0B5BA);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);

  // Discount Badge
  static const Color discountBadge = Color(0xFF00C853);

  // Shadow
  static Color shadow = Colors.black.withOpacity(0.08);
}
